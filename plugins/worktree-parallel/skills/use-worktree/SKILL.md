---
name: use-worktree
description: Switch to the `worktree` agent and start using it for parallel worktree orchestration tasks.
user-invocable: true
---

# Use Worktree Agent

## Purpose

Use this skill to switch to the `worktree` agent and continue the request there.

## Workflow

1. Switch to the `worktree` agent.
2. Continue the current task in that agent's scope.
