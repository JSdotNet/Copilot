{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "prompt",
        "prompt": "When creating or editing Copilot customization assets, enforce naming and frontmatter rules: agent files as role.agent.md, instruction files as *.instructions.md with applyTo and description, skill files as SKILL.md with name and description, and plugin manifests with valid component path mappings."
      }
    ]
  }
}
