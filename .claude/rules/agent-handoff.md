---
paths:
  - ".github/agents/**/*.md"
---

# Agent Handoff Approval Instructions (Claude Loader)

Read [.agents/rules/agent-handoff.md](../../.agents/rules/agent-handoff.md)
and follow it. The rules live in that file so both hosts load one shared copy;
this file only carries the Claude `paths`.
